group 'com.example'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    implementation project(':utils')
    testImplementation 'org.junit.jupiter:junit-jupiter:5.8.1' // Add JUnit Jupiter dependency for testing
}

test {
    useJUnitPlatform()
}

compileJava {}.dependsOn(':utils:publishToMavenLocal')